[package]
name = "ludtwig"
version = "0.1.0"
authors = ["Malte Janz <malte.janz@yahoo.de>"]
license = "MIT"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[profile.release]
lto = true # does increase runtime performance for longer build times
#only for profiling! remove for release
#debug = 1

[dependencies]
twig = { path="libs/twig" }
clap = "~3.0.0-beta.2"
tokio = { version = "~1.0.1", features = ["rt-multi-thread", "sync", "fs", "io-util", "macros"] }
ansi_term = "~0.12.1"
walkdir = "~2.3.1"
